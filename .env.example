# Required: Telegram bot token from BotFather.
TELEGRAM_BOT_TOKEN=

# Required: Fernet key used to encrypt stored survey data.
# Generate with:
# python3 -c "from cryptography.fernet import Fernet; print(Fernet.generate_key().decode())"
ENCRYPTION_KEY=

# Required: secret salt used for HMAC-SHA-256 pseudonymous user IDs.
# Store and back this up next to ENCRYPTION_KEY.
UKRAINABILITY_USER_HASH_SALT=

# Optional key-rotation form. First key encrypts new data; later keys decrypt
# historical data during rotation.
# ENCRYPTION_KEYS=active-fernet-key,retiring-fernet-key

# Optional: storage directory for the SQLite DB and encrypted voice files.
# Defaults to /home/ubuntu/kremenchuk for compatibility with the current deployment.
# UKRAINABILITY_STORAGE_DIR=/home/ubuntu/kremenchuk

# Optional: legacy credentials file with lines like:
# export TELEGRAM_BOT_TOKEN="..."
# export ENCRYPTION_KEY="..."
# export UKRAINABILITY_USER_HASH_SALT="..."
# Defaults to /home/ubuntu/kremenchuk/secure/credentials
# UKRAINABILITY_CREDENTIALS_FILE=/home/ubuntu/kremenchuk/secure/credentials

# Optional: log file names and rotation limits.
# UKRAINABILITY_BOT_ERRORS_LOG=bot_errors.log
# UKRAINABILITY_FLOW_CONTROL_LOG=flow_control.log
# UKRAINABILITY_LOG_MAX_BYTES=5000000
# UKRAINABILITY_LOG_BACKUP_COUNT=5

# Optional: encrypted voice-file retention and cleanup cadence.
# UKRAINABILITY_VOICE_RETENTION_DAYS=30
# UKRAINABILITY_CLEANUP_INTERVAL_SECONDS=86400
